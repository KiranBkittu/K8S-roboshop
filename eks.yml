apiversion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: Test12345
  region: us-east-1
managedNodeGroups:
  - name: roboshop-dev
    instanceTypes: ["t3.small","t3.medium"]
    # if not works will use m7i-flex.large but it is costly
    desiredCapacity: 2  # default is 3
    spot: true  # if we keep spot: true it reduces cost
   
